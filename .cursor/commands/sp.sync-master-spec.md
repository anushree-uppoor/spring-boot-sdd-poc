---
description: Sync or reconcile a master spec with feature specs (placeholder; extend as needed).
---

## User Input

```text
$ARGUMENTS
```

## Roam Code (MCP)

When **`roam_*` tools** are available, you **MUST** call **`roam_map`** once before merging or reconciling spec content so cross-feature references match the repo. If unavailable, proceed. **Disclosure:** one line `Roam MCP: …` in the final summary.

## Outline

Define workflow steps here when this command is fully specified.
